apiVersion: v1
kind: Service

metadata:
  name: shoprupee
  namespace: shoprupee

spec:
  type: NodePort

  selector:
    app: shoprupee

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
